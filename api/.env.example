# ZK Private Lending API - Environment Variables

# ============ Server ============
PORT=3001
ENVIRONMENT=development  # development | staging | production

# ============ Database ============
# PostgreSQL 연결 문자열
DATABASE_URL=postgres://postgres:postgres@localhost:5432/zk_lending

# ============ External Services ============
# 가격 오라클 URL (mock 또는 실제 서비스)
PRICE_ORACLE_URL=http://localhost:3002/price

# Ethereum RPC (로컬 Anvil 또는 Infura/Alchemy)
ETH_RPC_URL=http://localhost:8545

# ============ ZK Prover (Optional) ============
# Proving Key 파일 경로 (없으면 생성)
# PROVING_KEY_PATH=/path/to/proving_key.bin

# ============ Logging ============
# RUST_LOG=debug
# RUST_LOG=zk_lending_api=debug,tower_http=debug,sqlx=warn
